node_modules/
*.log
.DS_Store

# One-off diagnostics. The probes worth keeping have real names and are cited
# from HANDOFF.md; anything prefixed with _ was written to answer one question
# on one afternoon and is not maintained.
tmp/
tests/qa/_*.mjs
dist-desktop/

# The GitHub Pages artifact. Built from the canonical source by
# `npm run pages:build`, never edited, never committed — a checked-in copy of
# the game is a second Sovereign Scar waiting to drift from the first.
dist-pages/

